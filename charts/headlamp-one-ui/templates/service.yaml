apiVersion: v1
kind: Service
metadata:
  name: {{ include "headlamp-one-ui.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
  selector:
    app: {{ include "headlamp-one-ui.name" . }}
